# Chiffon Production Deployment Configuration
# Location: /mnt/user/appdata/chiffon/.env (on Unraid)
#
# Copy this file to .env and fill in actual values before deployment
# Secrets should never be committed to git

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

DB_USER=agent
DB_PASSWORD=change-me-to-strong-password
DB_NAME=agent_deploy

# Full connection string (auto-built from above, but can override)
# DATABASE_URL=postgresql://agent:password@postgres:5432/agent_deploy

# ============================================================================
# MESSAGE BROKER (RabbitMQ)
# ============================================================================

RABBITMQ_USER=guest
RABBITMQ_PASSWORD=change-me-to-strong-password

# ============================================================================
# LLM PROVIDERS - REQUIRED
# ============================================================================

# Anthropic Claude API (primary LLM)
# Get key from: https://console.anthropic.com/account/keys
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# OpenAI GPT-4 (fallback LLM, optional for MVP)
# Get key from: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# ============================================================================
# LITELLM PROXY CONFIGURATION
# ============================================================================

# Master key for LiteLLM proxy authentication
LITELLM_MASTER_KEY=change-me-to-strong-secret-key

# ============================================================================
# OLLAMA CONFIGURATION
# ============================================================================

# Remote Ollama service (running on GPU machine)
# spraycheese.lab.klsll.com has RTX 5080 for fast inference
OLLAMA_HOST=spraycheese.lab.klsll.com
OLLAMA_PORT=11434

# Full URL: http://spraycheese.lab.klsll.com:11434
# Auto-built from OLLAMA_HOST and OLLAMA_PORT in docker-compose

# ============================================================================
# LOGGING
# ============================================================================

LOG_LEVEL=INFO
# DEBUG for verbose logging (use only for troubleshooting)
# INFO for normal operation
# WARNING to reduce noise

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
#
# Security Best Practices:
# 1. Change all default passwords before production deployment
# 2. Use strong passwords (20+ chars, mixed case, numbers, symbols)
# 3. Restrict access to .env file: chmod 600 .env
# 4. Never commit .env to git
# 5. Rotate API keys regularly
#
# To generate strong password:
#   openssl rand -base64 32
#
# Testing credentials (development only):
# DB_PASSWORD=password
# RABBITMQ_PASSWORD=guest
# LITELLM_MASTER_KEY=dev-key
# These WILL be changed before production deployment
#
# ============================================================================
